# This file controls the AWS CodeBuild

version: 0.1
phases:
  build:
    commands:
      - echo Build started on `date`       
  post_build:
    commands:
      - echo Build completed on `date`      
artifacts:
  files:
    - target/*.war
  discard-paths: yes