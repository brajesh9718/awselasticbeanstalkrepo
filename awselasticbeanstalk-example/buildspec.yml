version: 0.2
phases:
  pre-build: null
  commands:
  - echo "Entered the prebuild phase"
  build:
    commands:
    - echo "Entered build phase"
    - mvn clean install
  post-build:
    commands:
    - echo "Entered the post build phase"